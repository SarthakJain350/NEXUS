# NEXUS R3 backend dependencies — kept separate from root requirements.txt
# (torch/onnxruntime pins there conflict; see Plan §0). Python 3.11.
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
psycopg[binary]>=3.2
alembic>=1.13
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
httpx>=0.27
pytest>=8.2
pytest-asyncio>=0.23
